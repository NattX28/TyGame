export type Person = {
  name: string;
  avatar: string;
};

export interface ProfileData {
  username: string;
  fullName: string;
  avatar: string;
  posts: number;
  friends: number;
  bio: string;
}
