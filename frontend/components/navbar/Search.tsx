import { Input } from "../ui/input";

const Search = () => {
  return (
    <Input
      type="text"
      placeholder="Search community...."
      className="w-96 max-w-xs"
    />
  );
};
export default Search;
